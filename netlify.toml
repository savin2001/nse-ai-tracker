[build]
  base    = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — all routes served from index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
